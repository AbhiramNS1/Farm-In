import 'package:flutter/material.dart';

class FarmersProfile extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Column(
      children: [Text("farmers profile")],
    );
  }
}
